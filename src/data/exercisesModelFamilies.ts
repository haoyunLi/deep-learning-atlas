import type { Exercise } from "./exerciseTypes";

type Prompt = [
  question: string,
  correct: string,
  distractor1: string,
  distractor2: string,
  explanation: string,
];
type Pair = [lessonId: string, mechanism: Prompt, decision: Prompt];

const pairs: Pair[] = [
  [
    "xgboost",
    [
      "XGBoost 某叶 G=4、H=2、λ=1，η=0.3，这棵树对叶内预测的增量是多少？",
      "−0.4",
      "−1.2",
      "+0.4",
      "叶值 w=−G/(H+λ)=−4/3，再乘 η=0.3，增量为 −0.4。正梯度表示当前预测沿正方向会增加损失，所以本次修正为负。",
    ],
    [
      "训练误差持续降低，验证误差先降后升，且最佳轮次已过去，怎样选模型？",
      "依据独立验证的最佳轮次早停，并检查树深和叶约束",
      "继续加树，保证最终验证误差一定下降",
      "用测试集逐轮选最佳树数，再将它称作独立评估",
      "Boosting 逐轮加容量可能过拟合。早停轮次属于模型选择，应由验证集确定；深度、叶约束与正则可以限制后续模型复杂度。",
    ],
  ],
  [
    "gcn",
    [
      "路径 1—2—3 加自环后度数是 [2,3,2]，特征为 [2,0,4]，W=1。对称归一化 GCN 给中心节点的激活前结果是多少？",
      "6/√6≈2.45",
      "(2+0+4)/3=2",
      "2+0+4=6",
      "S₂₁=S₂₃=1/√(3×2)，S₂₂=1/3，因此结果是 2/√6+0/3+4/√6。对称归一化并非一般意义上的行均值。",
    ],
    [
      "要评估未来新论文分类，训练时可以使用未来才形成的引用关系吗？",
      "应只使用预测时刻已知的关系，并按时间构建图",
      "可以，因为引用边不是类别标签",
      "只要 GCN 不够深，就不会产生信息泄漏",
      "边本身携带信息。Transductive 协议允许完整已知图，不代表模拟未来节点时可以提前读取未来关系；评估信息边界必须匹配部署。",
    ],
  ],
  [
    "graphsage",
    [
      "每个目标节点采 10 个一跳邻居，每个一跳再采 5 个二跳，未去重时最多涉及多少节点位置（含自身）？",
      "61",
      "16",
      "100",
      "依赖数为 1+10+10×5=61，二跳扇出会相乘。实际去重可减少节点数，但不能把两层 fanout 简单相加估算内存。",
    ],
    [
      "GraphSAGE 要应用到新节点，哪项条件最关键？",
      "新节点有可获得且语义一致的特征与邻域，供共享聚合器处理",
      "每个新节点必须在训练期已有独立 ID embedding",
      "把采样 fanout 增大就能保证泛化到任意新类型节点",
      "归纳能力来自共享的特征聚合函数。采样控制成本，但无法弥补新节点缺少信息或特征、关系语义改变的问题。",
    ],
  ],
  [
    "gat",
    [
      "某 GAT 头的两个候选分数是 0 与 ln3，变换后特征为 1 与 3，softmax 加权和是多少？",
      "2.5",
      "2",
      "4",
      "exp(0):exp(ln3)=1:3，权重是 0.25 与 0.75，加权和为 0.25×1+0.75×3=2.5。归一化范围是该目标节点的候选邻居。",
    ],
    [
      "一个邻居 attention 权重很高，能据此宣称它对分类结果具有因果作用吗？",
      "不能；应通过扰动、移除边等检查稳定性，因果解释还需额外假设",
      "能，因为 softmax 权重之和等于 1",
      "能，只要有超过一个 attention head",
      "注意力权重描述这一层如何汇集可见消息。相关特征、可替代邻居和后续层也会改变结果，权重大小本身并非因果效应。",
    ],
  ],
  [
    "time-series-forecasting",
    [
      "小时级预测使用 168 小时历史、5 个变量，直接输出未来 24 小时单目标，输入/输出形状应是哪组？",
      "[B,168,5] → [B,24,1]",
      "[B,24,1] → [B,168,5]",
      "[168,B,24] → [5,B,1]",
      "B 是 batch，历史时间轴长度 L=168，输入通道 F=5；直接多步输出时间轴为 H=24，目标通道为 1。形状应与框架约定一起核对。",
    ],
    [
      "实际部署只能获得未来天气预报，回测时应使用哪个天气输入？",
      "每个预测起点当时发布的天气预报版本",
      "未来真实天气，因为它测得更准确",
      "在整个测试期拟合后平滑的天气序列",
      "回测应模拟信息可用时间。未来实际天气和之后修订的预报包含当时未知的信息；已知日历与未知未来观测必须区分。",
    ],
  ],
  [
    "ddpm",
    [
      "DDPM 中 x₀=2、ε=−1、ᾱₜ=0.36，闭式加噪的 xₜ 等于多少？",
      "0.4",
      "−0.2",
      "1.36",
      "xₜ=√0.36×2+√0.64×(−1)=1.2−0.8=0.4。注意公式使用累计 ᾱₜ，且两个系数都要开平方。",
    ],
    [
      "把 ε prediction checkpoint 接到期望 v prediction 的 sampler 后图像失真，应先做什么？",
      "匹配预测类型、转换公式和 schedule，再评估采样质量",
      "只增加采样步数就一定解决",
      "将网络输出直接当 RGB 像素显示",
      "ε、x₀、v 是不同的输出参数化。采样器误读网络输出会用错更新方向；接口一致性必须先于步数和质量调参。",
    ],
  ],
  [
    "ddim",
    [
      "η=0，当前 xₜ=0.4、ᾱₜ=0.36、ε̂=−1，下一点 ᾱₛ=0.64。DDIM 的 xₛ 是多少？",
      "1.0",
      "2.0",
      "0.4",
      "先算 x̂₀=(0.4+0.8)/0.6=2，再算 xₛ=0.8×2+0.6×(−1)=1。中间点仍保留与其噪声水平匹配的方向项。",
    ],
    [
      "已有兼容的 DDPM 噪声预测网络，希望减少生成调用数，合理路线是什么？",
      "使用匹配配置的 DDIM sampler，并比较少步质量与延迟",
      "必须先把所有网络层换成新架构",
      "直接删掉原循环中的步骤但保留相邻步公式",
      "DDIM 的实践优势之一是可复用兼容去噪器。跳步需要适配起终时间的更新系数，不能只删除循环；少步是否足够需实际评估。",
    ],
  ],
  [
    "latent-diffusion",
    [
      "图像 [3,512,512] 编码为 [4,64,64]，不计 batch，总标量数缩小多少倍？",
      "48 倍",
      "64 倍",
      "8 倍",
      "(3×512×512)/(4×64×64)=48。边长缩小 8 倍，空间位置少 64 倍，但通道从 3 变成 4，所以总标量压缩是 48 倍。",
    ],
    [
      "生成图中的细字总丢失，真实图做 encode→decode 也丢同样细字，优先检查什么？",
      "Autoencoder 的压缩与重建瓶颈",
      "只把 DDIM 步数加到最大",
      "把 latent 当 RGB 并跳过 decoder",
      "重建已暴露信息瓶颈。冻结的 decoder 无法可靠还原 encoder 未保留的细节；增加去噪步数不能直接修复这一阶段的问题。",
    ],
  ],
  [
    "flow-matching",
    [
      "直线条件路径使用 z=−1、x=3、t=0.25，输入位置和目标速度分别是什么？",
      "xₜ=0，u=4",
      "xₜ=0，u=3",
      "xₜ=1，u=−4",
      "xₜ=(1−0.25)(−1)+0.25×3=0，导数 u=x−z=4。此处时间从噪声走向数据，改变时间方向时速度符号也需相应改变。",
    ],
    [
      "条件训练路径都是直线，是否能保证训练后的模型一步生成高质量数据？",
      "不能，边缘速度场与数值积分仍可能需要多次网络调用",
      "能，直线插值说明网络不需要积分",
      "能，因为训练时已将真实终点作为推理输入",
      "不同条件路径会交叠，回归学到的是当前位置和时间下的平均方向。边缘轨迹不保证为直线，且推理看不到真实终点，少步能力需验证或额外训练。",
    ],
  ],
  [
    "off-policy-evaluation",
    [
      "四条日志的 importance weights 为 [4,0,0,0]，ESS=(Σρ)²/Σρ² 等于多少？",
      "1",
      "4",
      "16",
      "Σρ=4、Σρ²=16，所以 ESS=16/16=1。点估计几乎依赖单条观测；ESS 用于诊断权重集中，不等于置信区间或因果识别证明。",
    ],
    [
      "目标策略会在某状态选 A，但旧策略 μ(A|s)=0，应如何解释该区域的 OPE？",
      "日志缺少该动作支持，需补充数据、限制目标策略或明确额外假设",
      "把分母加 10⁻⁶ 就得到可靠无偏结果",
      "改用 DR 后自动恢复从未观察过的结果",
      "Support 缺失是信息问题。人为加 ε 只改变数值；DR 也不能在无额外假设下识别未观察动作的价值，更不能自动修复未测混杂。",
    ],
  ],
];

export const modelFamilyExercises: Record<string, Exercise[]> =
  Object.fromEntries(
    pairs.map(([id, ...questions], i) => [
      id,
      questions.map(([question, correct, wrong1, wrong2, explanation], j) => {
        const answer = (i + j) % 3;
        const options = [wrong1, wrong2];
        options.splice(answer, 0, correct);
        return {
          id: `${id}-${j === 0 ? "mechanism" : "decision"}`,
          kind: j === 0 ? "mechanism" : "decision",
          question,
          options,
          answer,
          explanation,
        };
      }),
    ]),
  );
